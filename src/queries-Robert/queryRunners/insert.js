/**
 *
 * @param query
 * @param dataBase
 * @param commitResults
 */
let executeInsert = (query, dataBase, commitResults) => {
    // TODO - INSERT
};

module.exports = {
    executeInsert: executeInsert
}
